package net.godzin.frostydelight.data.blocks;

import net.minecraft.core.HolderLookup;
import net.minecraft.data.loot.BlockLootSubProvider;
import net.minecraft.world.flag.FeatureFlags;
import net.minecraft.world.level.block.Block;

import java.util.Set;

/*
public class BlockLootTableProvider extends BlockLootSubProvider {
    public BlockLootTableProvider(HolderLookup.Provider registries) {
        super(Set.of(), FeatureFlags.REGISTRY.allFlags(), registries);
    }

    @Override
    protected void generate() {

    }

    // @Override
    // protected Iterable<Block> getKnownBlocks() {
    // Handle the registration of mod blocks//
    // return Blocks.BLOCKS.getEntries().stream().map(Holder::value)::iterator;
    // }
}
*/